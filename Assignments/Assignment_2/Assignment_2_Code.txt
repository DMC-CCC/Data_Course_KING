list.files(path = "Data/", pattern = "\\.csv$", recursive = TRUE, full.names = TRUE)
# This command lists all of the .csv files found in the Data/ directory (recursive)
# I initially only wrote this command for the .csv files found in the main Data/ directory
# But as I kept going, I decided to go back to change it to be recursive because the instruction
# is written for 'all' files. 
# This command also is working based on the fact that I'm already working within my desktop
# Data_Course_KING repository.

csv_files = list.files(path = "Data/", pattern = "\\.csv$", recursive = TRUE, full.names = TRUE) 
# This command takes that list and creates the object called csv_files

length(csv_files) 
# This command tells me how many files are in the object

df = read.csv("Data/wingspan_vs_mass.csv")
# This command creates the df data frame which reads and lists the specified file

head(df)
# This command lists the first six lines of the specified data frame

list.files(path = "Data/", pattern = "^b", recursive = TRUE, full.names = TRUE)
# This command lists any files (recursive) in the Data/ directory that begin with a lowercase b

b = list.files(path = "Data/", pattern = "^b", recursive = TRUE, full.names = TRUE)
# This command creates the object b which represents the 3 files found above

for (file in b) {
  first_line = readLines(file, n = 1, warn = FALSE)
  print(paste(file, ":", first_line))
}
# This command is a for-loop that lists the first line of each file contained within the object b
for (file in csv_files) {
  first_line = readLines(file, n = 1, warn = FALSE)
  print(paste(file, ":", first_line))
}
# This command is a for-loop that lists the first line of all .csv files within the Data
# repository (recursive)